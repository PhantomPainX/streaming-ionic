export class UserGroups {

    public moderator: boolean;
    public vip: boolean;

    // constructor(moderator: boolean, vip: boolean) {
    //     this.moderator = moderator;
    //     this.vip = vip;
    // }
}
